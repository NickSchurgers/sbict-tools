<UserControl x:Class="SBICT.Modules.Chat.Views.ChatList"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:viewModels="clr-namespace:SBICT.Modules.Chat.ViewModels"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="300"
             xmlns:prism="http://prismlibrary.com/"
             xmlns:module="clr-namespace:SBICT.Modules.Chat"
             prism:ViewModelLocator.AutoWireViewModel="True"
             d:DataContext="{d:DesignInstance viewModels:ChatListViewModel, IsDesignTimeCreatable=True}">
    <UserControl.Resources>
        <HierarchicalDataTemplate DataType="{x:Type module:ChatGroup}" ItemsSource="{Binding Items, UpdateSourceTrigger=PropertyChanged}">
            <Label Content="{Binding Name}" />
        </HierarchicalDataTemplate>
        <DataTemplate DataType="{x:Type module:Chat}">
            <Label Content="{Binding Name}"></Label>
        </DataTemplate>
    </UserControl.Resources>
    <Grid>
        <TreeView ItemsSource="{Binding ChatGroups, UpdateSourceTrigger=PropertyChanged}">
        </TreeView>
    </Grid>
</UserControl>